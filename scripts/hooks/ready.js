Hooks.once('ready', async function() {
    console.log("Hello World");
    /*if (!["dnd5e"].includes(game.system.id)) return;
    if (game.modules.get("times-up") ? .active && isNewerVersion(game.modules.get("times-up").data.version, "0.0.9")) {
        setupSpecialDurations();
    }
    
    await import ("./scripts/hooks/preUpdateActor.js");
    await import("./scripts/hooks/updateActor.js");*/
});